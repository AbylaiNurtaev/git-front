# AGENTS.md

## Development rules

- NEVER run `npm run build` after changes
- ALWAYS use `npm run dev` for development
- Do not perform production builds unless explicitly asked
- Prefer fast iteration over full builds

## Project commands

- Dev: `npm run dev`
- Build: `npm run build` (only for deployment)
